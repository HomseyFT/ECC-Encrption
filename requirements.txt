# No external dependencies required for core ECC.
# Python's built-in arbitrary-precision integers and `secrets` module
# handle the heavy lifting.
#
# Optional (uncomment if needed later):
# hashlib   — built-in, needed if you implement ECDSA (message hashing)
# secrets   — built-in, for cryptographically secure random numbers
# gmpy2     — optional, faster modular arithmetic for very large primes
pygame
